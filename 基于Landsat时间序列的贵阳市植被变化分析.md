## 项目背景
本项目利用Landsat系列卫星影像，分析2015年至2025年贵阳市植被覆盖的时空变化趋势。通过计算NDVI（归一化植被指数），提取时间序列，评估城市化进程中植被的演变特征。

---

## 数据准备
- 数据源：USGS EarthExplorer (https://earthexplorer.usgs.gov/)
- 传感器：Landsat 8 (2015-2021) + Landsat 9 (2022-2025)
- 数据级别：Level-2（已完成大气校正）
- 时间范围：2015年1月 - 2025年7月
- 空间范围：贵阳市行政区划范围（经纬度边界或导入KML）
- 筛选条件：
  - 云量 < 20%
  - 每年至少选取2景影像（夏季/秋季为主，确保植被生长季）
  - 尽量保持相近的月份（如每年8-9月），减少季节差异对趋势判断的影响

---

## 工具与环境
- Python 3.x
- 依赖库：rasterio, numpy, matplotlib, geopandas, pandas, seaborn（可选）
- 辅助工具：ArcGIS（可选，用于精细制图）
- 开发环境：PyCharm / VS Code / Jupyter Notebook（开发时可用Notebook快速验证，交付时整理为.py脚本或干净版Notebook，建议最终以.py为主）

---

## 任务清单

### 1. 数据下载
- 注册USGS EarthExplorer账号
- 在EarthExplorer中划定贵阳市范围（矩形框或导入KML）
- 按年份筛选符合条件的Landsat 8/9影像
- 下载每景影像对应的所有波段文件（或直接下载Level-2科学产品）
- 记录每景影像的日期、传感器类型、云量等元数据

### 2. 数据读取与预处理
- 使用rasterio或gdal读取GeoTIFF文件
- 提取红光波段（Band 4）和近红外波段（Band 5）
- 检查像元值范围（Level-2数据通常为反射率，范围0-1或缩放后整数）
- 若需要，进行裁剪（按贵阳市边界掩膜）
- 将结果输出为NDVI单波段GeoTIFF，便于后续复现和检查

### 3. 计算NDVI
- 对每景影像计算：NDVI = (NIR - Red) / (NIR + Red)
- 处理无效像元（如水体、云、阴影），设置有效值范围（-1到1）
- 输出NDVI空间分布图（可先以matplotlib快速查看，验证计算结果是否合理）
- 保存每景影像的NDVI结果（GeoTIFF或numpy数组）

### 4. 提取时间序列
- 在贵阳市范围内，划分中心城区和郊区
- 在每个区域内随机选取或均匀布设采样点（建议10-20个点）
- 提取每个采样点在不同年份的NDVI值
- 整理为结构化表格（行：采样点，列：年份/季节）
- 计算每个区域每年的NDVI均值、标准差等统计量，用于后续趋势判断

### 5. 统计分析
- 绘制时间序列折线图（横轴：年份，纵轴：NDVI均值）
- 绘制中心城区与郊区的NDVI变化对比图，观察差异
- 初步判断趋势：上升/下降/波动/稳定
- 标注可能的关键事件（如城市扩张、绿化工程等），方便分析驱动因素

### 6. 制图与可视化
- 生成2015年和2025年的NDVI空间分布对比图（或选择变化最明显的年份对）
- 可选：生成NDVI变化趋势图（如线性回归斜率图）
- 可选：使用ArcGIS对成果图进行精细调整，提升展示效果

### 7. 撰写分析报告
- 用英文撰写，300-500字
- 结构：
  - 背景与目的
  - 数据与方法
  - 结果
  - 结论
- 结论需包含至少一个具体的定量发现（如“中心城区NDVI下降X%”或“郊区NDVI增长Y%”）

---

## 交付物清单

1. 所有数据处理脚本（.py文件），包含：
   - 数据读取
   - NDVI计算
   - 时间序列提取
   - 统计分析与可视化
2. 时间序列折线图（成果图）
3. NDVI空间分布对比图（至少一张，2015 vs 2025或趋势图）
4. 英文分析报告（300-500字，放入README或单独Markdown文件）
5. 完整的项目README.md，包含：
   - 项目标题与概述
   - 数据来源与传感器信息
   - 方法论简述
   - 成果图展示
   - 核心结论

---

## 拓展方向（可选）

- 将NDVI替换为其他指数（如NDWI水体指数、NDBI建筑指数）进行补充分析
- 引入气象数据（降水、温度），分析植被变化的气候驱动因素
- 引入夜间灯光数据，分析城市化强度与植被变化的关系
- 尝试用机器学习方法（随机森林/时间序列分解）对NDVI趋势进行预测

---
