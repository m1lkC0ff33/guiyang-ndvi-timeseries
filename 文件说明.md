
---

## 文件职责说明

| 文件/目录 | 职责 |
|-----------|------|
| `README.md` | 项目首页，包含概述、数据来源、方法描述、结果展示、使用说明。作为GitHub仓库的门面，供访客和复试导师快速了解项目全貌。 |
| `LICENSE` | MIT开源协议，允许他人自由使用、修改、分发代码。 |
| `.gitignore` | 排除原始数据文件（.TIF）、缓存文件（__pycache__）、虚拟环境、IDE配置等，确保仓库整洁。 |
| `requirements.txt` | 记录Python依赖包及版本，用于快速安装环境：`pip install -r requirements.txt`。 |
| `data/raw/` | 存放从USGS下载的原始Landsat影像，不提交至Git（文件体积大），仅本地保留。 |
| `data/processed/` | 存放处理后的中间数据（如NDVI栅格、采样点矢量），可提交示例数据供他人复现。 |
| `src/config.py` | 集中管理所有路径、参数、常量（如年份列表、波段编号、采样点数量），其他脚本统一从此导入，便于修改和维护。 |
| `src/download.py` | 记录每景影像的元数据信息（下载日期、传感器、云量、URL），不是自动化下载工具，仅用于记录数据溯源。 |
| `src/preprocess.py` | 读取原始影像，提取Band 4和Band 5，按贵阳市边界裁剪，输出预处理后的波段数据。 |
| `src/ndvi_calc.py` | 读取预处理后的波段数据，计算NDVI，输出单波段GeoTIFF文件。 |
| `src/timeseries.py` | 读取所有年份的NDVI栅格，在采样点位置提取NDVI值，整理为时间序列表格。 |
| `src/analyze.py` | 对时间序列数据进行统计计算（均值、标准差、趋势斜率、显著性检验），输出统计结果。 |
| `src/visualize.py` | 读取统计结果和NDVI栅格，生成时间序列折线图、空间分布对比图等成果图。 |
| `notebooks/` | 存放开发阶段的Jupyter Notebook，用于快速验证和演示，交付前需清理多余输出。 |
| `outputs/figures/` | 存放最终成果图（PNG/PDF格式），用于README展示和复试材料。 |
| `outputs/tables/` | 存放统计结果表格（CSV格式），便于数据复查。 |
| `outputs/report.md` | 英文分析报告，包含背景、方法、结果、结论。 |
| `config/params.yaml` | YAML格式配置文件，存储路径、年份范围、采样参数等，供Python脚本读取。 |
| `tests/` | 单元测试目录，用于验证核心函数（如NDVI计算是否正确），非必须。 |